import { Routes } from '@angular/router';
import { AppComponent } from './app.component';

export const routes: Routes = [
  /**
   * STEP X: provide route for AppComponent
   */
  { path: '', component: AppComponent },
];
